package com.kevlar.rooting.dataset

/**
 * Defines a trait which can be targeted, analyzed and detected in the running system.
 * */
public enum class SystemTarget {
    ROOT, BUSYBOX, TOYBOX, MAGISK, XPOSED;
}